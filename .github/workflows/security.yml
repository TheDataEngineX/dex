name: Security Scans

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: TheDataEngineX/.github/.github/workflows/reusable-security.yml@main
    secrets: inherit
